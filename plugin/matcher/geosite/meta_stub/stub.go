package metastub

import (
	"fmt"

	"github.com/rnetx/cdns/utils/domain"
)

func ReadFile(path string) (*domain.DomainSet, int, error) {
	return nil, 0, fmt.Errorf("clash.meta geosite type is not supported")
}
