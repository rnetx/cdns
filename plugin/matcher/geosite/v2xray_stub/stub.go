package v2xraystub

import (
	"fmt"

	"github.com/rnetx/cdns/utils/domain"
)

func ReadRule(path string, codes []string) (map[string]*domain.DomainSet, error) {
	return nil, fmt.Errorf("v2xray geosite type is not supported")
}
